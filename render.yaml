services:
  # ─────────────────────────────────────────────
  # Backend — FastAPI / Python web service
  # ─────────────────────────────────────────────
  - type: web
    name: real-rails-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: CORS_ORIGINS
        value: https://real-rails-frontend.onrender.com
    healthCheckPath: /api/lineage

  # ─────────────────────────────────────────────
  # Frontend — Next.js / Node web service
  # ─────────────────────────────────────────────
  - type: web
    name: real-rails-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm install --legacy-peer-deps && npm install tailwindcss @tailwindcss/postcss postcss --legacy-peer-deps && npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://real-rails-backend.onrender.com
      - key: NODE_ENV
        value: production
      - key: NPM_FLAGS
        value: --legacy-peer-deps
    healthCheckPath: /